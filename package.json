{
  "name": "presidium",
  "version": "0.0.1",
  "description": "Jekyll based technical documentation template.",
  "authors": [
    {
      "name": "Paco Mendes",
      "email": "paco.mendes@spandigital.com"
    },
    {
      "name": "Dominic Follett-Smith",
      "email": "dominic@spandigital.com"
    }
  ],
  "scripts": {
    "clean": "rimraf dist/*",

    "copy:content": "cpx \"./content/**/*\" docs",
    "copy:media": "cpx \"./media/**/*\" docs/media",
    "copy:presidium": "cpx \".build/presidium-core/**/*\" docs",
    "copy:config": "cpx \"./_config.yml\" ./docs",
    "build": "npm run copy:presidium && npm run copy:content && npm run copy:media && npm run copy:config",

    "watch:content": "cpx --watch \"./content/**/*\" docs",
    "watch:media": "cpx --watch \"./media/**/*\" docs/media",
    "watch:presidium": "cpx --watch \".build/presidium-core/**/*\" docs",
    "watch:config": "cpx --watch \"./_config.yml\" ./docs",
    "serve": "npm run watch:presidium & npm run watch:content & npm run watch:media & npm run watch:config & cd ./docs && jekyll serve & wait",

    "gh-pages": "npm run clean && npm run build"
  },

  "license": "TBD",
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^2.5.4"
  },
  "dependencies": {
  }
}