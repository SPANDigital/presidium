{
  "name": "presidium",
  "version": "0.0.1",
  "description": "A Jekyll based project using Presidium technical documentation templates.",
  "config": {
    "core" : "node_modules/presidium-core",
    "cooldown" : "2"
  },
  "authors": [
    {
      "name": "Paco Mendes",
      "email": "paco.mendes@spandigital.com"
    },
    {
      "name": "Dominic Follett-Smith",
      "email": "dominic@spandigital.com"
    }
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/SPANDigital/presidium"
  },
  "license": "Apache-2.0",

  "scripts": {

    "check:requirements" : "(cd $npm_package_config_core/bin && chmod +x requirements.sh && ./requirements.sh)",
    "install:directories" : "mkdir -p dist",
    "install:gems" : "(cd .jekyll && bundle install --path=.bundle --deployment)",
    "update:gems" : "(cd .jekyll && bundle update)",
    "install": "npm run check:requirements && npm run install:directories && npm run install:gems",

    "clean": "rm -rf dist && mkdir dist",
    "copy:presidium": "cpx \"$npm_package_config_core/**/*\" dist/src",
    "copy:content": "cpx \"./content/**/*\" dist/src",
    "copy:media": "cpx \"./media/**/*\" dist/src/media",
    "copy:config": "cpx \"./_config.yml\" dist/src",
    "copy:clean" : "rm -f dist/src/{package.json,LICENSE,NOTICE,README.md} && rm -fR dist/src/bin/",
    "copy": "npm run copy:presidium & npm run copy:content & npm run copy:media & npm run copy:config",

    "build:jekyll": "(cd .jekyll && bundle exec --verbose jekyll build -s ../dist/src -d ../dist/site)",
    "build": "npm run copy && npm run copy:clean && npm run build:jekyll",

    "watch:presidium": "cpx --watch \"$npm_package_config_core/**/*\" dist/src",
    "watch:content": "cpx --watch \"./content/**/*\" dist/src",
    "watch:media": "cpx --watch \"./media/**/*\" dist/src/media",
    "watch:config": "cpx --watch \"./_config.yml\" dist/src",


    "watch": "npm run watch:presidium & npm run watch:content & npm run watch:media & npm run watch:config",
    "serve" : "(cd .jekyll && bundle exec jekyll serve --port 4001 --incremental -s ../dist/src -d ../dist/site)",
    "start": "npm run watch & sleep $npm_package_config_cooldown && echo LOADED WATCHES...  && npm run serve",

    "gh-pages": "npm run clean && npm run build && git-directory-deploy --directory dist/src"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "rimraf": "^2.5.4",
    "git-directory-deploy": "^1.5.1"
  },
  "dependencies": {
    "presidium-core" : "^0.0.28"
  }
}
